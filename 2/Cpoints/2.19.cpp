#include <iostream>
#include <cmath>

using namespace std;

int main(){
	cout << "4 / 3 *(r+34) - 9(a+bc) +(3 + d * (2 + a))/(a + b * d)" << endl;
	cout << "5.5 * power((r + 2.5), 2.5 + t)" << endl;
	
	return 0;
}
